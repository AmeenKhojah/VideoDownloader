FROM python:3.11-slim

# Install system dependencies (FFmpeg)
RUN apt-get update && apt-get install -y ffmpeg
RUN echo "FFmpeg Installation Check:" && which ffmpeg

# Set working directory
WORKDIR /app

# Copy project files
COPY . .

# Expose port
EXPOSE 8080

# Start the app
CMD ["python", "app.py"]
